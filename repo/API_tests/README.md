# Deprecated

Integration tests have moved to `fullstack/backend/tests/Integration/`.
Legacy test files are archived at `fullstack/legacy_tests/API_tests/`.

Run tests: `cd fullstack/backend && vendor/bin/phpunit --testsuite integration`
