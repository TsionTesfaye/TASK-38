# Deprecated: Legacy API Structure Tests

These files test request/response structure and validation rules inline without
making real HTTP calls or instantiating real services. They are NOT run by
phpunit and are superseded by the integration tests in
`fullstack/backend/tests/Integration/`.

Do not add new tests here. Use `fullstack/backend/tests/Integration/` instead.
