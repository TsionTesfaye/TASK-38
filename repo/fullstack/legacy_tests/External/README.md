# Deprecated: Logic-Replica Tests

This directory contains auto-generated logic-replica tests that duplicate enum
and arithmetic checks without exercising real service classes. They have been
moved out of the phpunit test tree and are not executed.

Real service-level tests live in `tests/Unit/Service/` and `tests/Unit/Security/`.

These files are retained for reference only and will be removed in a future
cleanup pass.
