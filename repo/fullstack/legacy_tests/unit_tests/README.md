# Deprecated: Legacy Logic-Replica Tests

These files are logic-replica tests that reimplement business rules inline
without calling real service classes. They are NOT run by phpunit and are
superseded by the real service tests in `fullstack/backend/tests/`.

Do not add new tests here. Use `fullstack/backend/tests/Unit/Service/` instead.
