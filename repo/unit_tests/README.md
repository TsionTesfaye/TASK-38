# Deprecated

Unit tests have moved to `fullstack/backend/tests/Unit/`.
Legacy test files are archived at `fullstack/legacy_tests/unit_tests/`.

Run tests: `cd fullstack/backend && vendor/bin/phpunit --testsuite unit`
